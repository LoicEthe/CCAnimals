<header>
    <nav>
        <div class="bandeau">
            <p>Retrouvez des produits de qualité pour vos plus fidèles compagnons</p>
        </div>
    </nav>
    <nav class="navbar navbar-expand-md navbar-dark ">
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>
                <div class="dropdown">
                    <a class="btn"  href="{{ path('product_chien') }}" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Collection Chien
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('accessoires_chien') }}">Accessoires</a>
                        <a class="dropdown-item" href="{{ path('soins_chien') }}">Hygiènes et soins</a>
                        <a class="dropdown-item" href="{{ path('jouet_chien') }}">Jouets</a>
                        <a class="dropdown-item" href="{{ path('alim_chien') }}">Alimentation</a>
                        <a class="dropdown-item" href="{{ path('voyage_chien') }}">Voyage</a>
                        <a class="dropdown-item" href="{{ path('product_chien') }}">Voir tous les produits</a>
                    </div>
                </div>
                <div class="dropdown">
                    <a class="btn" href="{{ path('product_chat') }}" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Collection Chat
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <a class="dropdown-item" href="{{ path('accessoires_chat') }}">Accessoires</a>
                        <a class="dropdown-item" href="{{ path('soins_chat') }}" >Hygiènes et soins</a>
                        <a class="dropdown-item" href="{{ path('jouet_chat') }}">Jouets</a>
                        <a class="dropdown-item" href="{{ path('alim_chat') }}">Alimentation</a>
                        <a class="dropdown-item" href="{{ path('voyage_chat') }}" >Voyage</a>
                        <a class="dropdown-item" href="{{ path('product_chat') }}">Voir tous les produits</a>
                    </div>
                </div>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('products') }}">Tous nos produits</a>
                </li>
            </ul>
            <div class="btn-group dropleft">
                <button type="button" class="btn-user" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <img src="{{ asset('assets/img/user.png') }}"  alt="user" class="icons">
                </button>
                <div class="dropdown-menu">
                    {% if app.user %}
                        <a class="dropdown-item" href="{{ path('account') }}">Mon compte </a> |  <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% else %}
                        <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a> | <a class="dropdown-item" href="{{ path('register') }}">Inscription</a>
                    {% endif %}
                </div>
            </div>
            <a  href="{{ path('cart') }}">
                <img src="{{ asset('assets/img/shopping-bag.png') }}"  alt="Mon panier" class="icons">
            </a>

        </div>
    </nav>
</header>