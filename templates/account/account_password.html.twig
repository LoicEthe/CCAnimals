{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe!{% endblock %}

{% block content %}
    <h1 style="text-align: center">Modifier mon mot de passe</h1>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}

{% endblock %}
